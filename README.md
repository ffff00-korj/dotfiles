# Just my dotfiles

### Maybe you can find here something interesting

#### Installation

To install something, just look at a Makefile and run some commands

```sh
make all # install all

make install-zsh # copy .zshrc to you
make install-tmux # copy .tmux.conf
```
